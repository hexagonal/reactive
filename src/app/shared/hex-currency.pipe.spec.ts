import { HexCurrencyPipe } from './hex-currency.pipe';

describe('HexDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new HexCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
